---
title: "ATELIER 4 : MINES"
subtitle: ""
author: "GUEDE	Ogho Irène, SORHO	Foungotrigué Drissa, YAO	Amani Patrick Stéphane"
date: "2025-05-27"
date-format: iso
lang: fr
format:
  html:
    embed-resources: true
    smooth-scroll: true
    fontsize: 0.9em
    toc: true
    toc-depth: 3
    toc-title: "."
    bibliography: [references.bib]
    crossrefs-hover: false
    css: custom.css
    theme: [yeti, style.scss]
execute:
  warning: false
  message: false 
  echo: false
knitr:
  opts_chunk:
    out.width: "100%"
editor_options: 
  chunk_output_type: console
editor: 
  markdown: 
    wrap: 72
---


> Exemple de traitement réalisé par les groupes d'auditeur à partir de l'enquête AfroMapR.

# 1. Préparation des données

```{r}
don <- read.table(file = "data/Marché_Bouaké/data/data_clean.csv",  # nom du fichier et chemin d'accès
                  sep = ";",                     # séparateur (ici, des points-virgule)
                  dec=",",                       # Type de décimale
                  header = TRUE,                 # ligne d'en-tête avec le nom des variables
                  encoding="UTF-8",              # encodage adapté au français
                  stringsAsFactors = TRUE)       # Transforme les character en factor     

```


## Choix des variables à analyser

On sélectionne dans le tableau une variable Y que l'on veut expliquer et une variables X  qui peut servir à expliquer Y. On décide par exemple d'examiner les variables suivantes :

- **Y : mines**       : opinion globale sur l'exploitation minière
- **X : âge**         : âge en trois classes



```{r}
Y<-don$mines
nameY <- "Opinion sur l'exploitation minière"
X<-don$age
nameX <- "Classe d'âge"
sel<-data.frame(X,Y)
titre <- "Réalisation de l'atelier 4 : mines"

sel <-sel[complete.cases(sel), ]


```

##  Recodage éventuel des étiquettes

On peut raccourcir ou modifier les étiquettes pour faciliter leur affichage ou fusionner des modalités trop peu nombreuses. Ici on a décidé de regrouper les opinions en trois classes au lieu de cinq au départ car l'échantillon est petit. Uant aux classes d'âges on les a regroupées en deux classes seulement : 18-44 ans et 45 ans et plus

```{r}
levels(sel$X)<-c("18 à 44 ans","18 à 44 ans","45 ans et +")
levels(sel$Y)<-c("Négatif","Négatif","Neutre","Positif","Positif")
```


# 2. Analyse de la variable à expliquer Y


## Fréquence simple 

```{r}
library(knitr)
mytab2 <-table(sel$Y)
kable(addmargins(mytab2),col.names=c("Modalité","Fréquence"))
```

## Pourcentage 


```{r}
pct <- 100*prop.table(mytab2)
kable(addmargins(pct),col.names=c("Modalité","%"), digits=1)
```


## Diagramme en bâtons


```{r}
barplot(mytab2, 
        main = titre,
        sub = "Source : Enquête AfroMapR, Bouaké, 2025",
        ylab = nameY,
        col = c("pink","lightyellow","lightblue"))
```


# 3. Analyse de la variable explicative X


## Fréquence simple 


```{r}
mytab1 <-table(sel$X)
kable(addmargins(mytab1),col.names=c("Modalité","Fréquence"))
```

## Pourcentage 

```{r}
pct<-100*prop.table(mytab1)
kable(addmargins(pct),col.names=c("Modalité","%"), digits=1)
```


## Diagramme en bâtons


```{r}
barplot(mytab1, 
        main = titre,
        sub = "Source : Enquête AfroMapR, Bouaké, 2025",
        ylab = nameX,
        col = c("lightyellow","yellow","orange"))
```



# 4. Relation entre X et Y


## Hypothèses

On veut tester l'existence d'une relation entre les deux variables précédentes. 

- **H0** : Il n'y a pas de relation entre X et Y
- **H1** : Il y a une relation entre X et Y

- **Justification de l'hypothèse H1** : On suppose que les personnes jeunes sont plus favorables à l'exploitation minière que les personnes âgées car elles y voient une opportunité de travail et d'enrichissement.


## Tableau de contingence


```{r}
tabcont<-table(sel$X,sel$Y)   # Créer le tableau de contingence
tabtot <- addmargins(tabcont) # Ajouter les sommes en lignes et en colonne
kable(tabtot )                       # Afficher
```


## Pourcentage en ligne

```{r}
tabpct <- 100*prop.table(addmargins(tabcont,1),1) # Calcule le pourcentage en ligne (1)

kable(addmargins(tabpct,2), digits=1)                    # Arrondi à un chiffre après la virgule
```

On constate que la relation ne suit pas vraiment les prédictions. C'est même l'inverse puisque ce sont les plus vieux qui donnent les avis les plus positifs sur l'exploitation minière.

## Graphique en mosaïque 

```{r}
mosaicplot(tabcont, 
           main= titre,
           sub = "Source : Enquête AfroMapR, Bouaké, 2025",
           xlab = nameX,
           ylab = nameY,
        col = c("pink","lightyellow","lightblue"))
```


## Test du chi-2


On part de l'hypothèse nulle H0 : il n'y a pas de relation entre X et Y. Les variations en Y en fonction de X sont l'effet du hasard. 


On effectue un test du chi-2 qui donne les résultats suivants :

```{r}
chisq.test(tabcont)
```


## Conclusion

Il n'est pas possible de rejeter l'hypothèse H0 puisque la p-value indique que la relation entre les deux variable a 7% de chances d'être l'effet du hasard. 

On est tout de même assez proche su seuil de rejet de H0 (p < 0.05) et on pourrait tenter d'accroître la taille de l'échantillon pour faire un nouveau test qui serait peut-être significatif.  

